# Embeddings

Turn text into vectors — the building block for semantic search and RAG.
Models: `jina-embeddings-v3`, `qwen3-embedding-8b` / `4b` / `0.6b`.

```bash
pip install openai
export ECOHASH_API_KEY=eco_...   # create one at console.ecohash.com
python embed.py
```
