# Image generation

Generate an image from a text prompt and save it as a PNG. Model: `z-image-turbo`.

```bash
pip install openai
export ECOHASH_API_KEY=eco_...   # create one at console.ecohash.com
python generate.py
```
