# Vision

Ask a question about an image (pass a URL or a base64 data URI).
Models: `qwen3-vl-8b-instruct`, `gemma-4-31b-it`.

```bash
pip install openai
export ECOHASH_API_KEY=eco_...   # create one at console.ecohash.com
python describe_image.py
```
